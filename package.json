{
  "name": "@yellowgarbagebag/snow-white-dto",
  "version": "2.0.1",
  "description": "SnowWhite DTO",
  "homepage": "https://gitlab.com/YellowGarbageBag/rest-api-dto",
  "bugs": {
    "url": "https://gitlab.com/YellowGarbageBag/rest-api-dto/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@gitlab.com/YellowGarbageBag/rest-api-dto.git"
  },
  "license": "ISC",
  "author": "Steffen",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "scripts": {
    "prebuild": "rm -rf dist",
    "build": "tsc",
    "build:watch": "nodemon -x npm run build --ignore dist -i *.spec.ts -e ts",
    "format": "prettier --write \"src/**/*.ts\"",
    "lint": "eslint --ext .ts src/",
    "lint:fix": "npm run lint -- --fix",
    "publish:patch": "npm run lint && npm run build && npm version patch && npm publish && git push --tags"
  },
  "prettier": "@hest-lab/eslint-config",
  "dependencies": {},
  "devDependencies": {
    "@hest-lab/eslint-config": "^1.2.0",
    "@types/node": "^14.14.7",
    "eslint": "^7.13.0",
    "prettier": "^2.1.2",
    "typescript": "^4.0.5"
  },
  "publishConfig": {
    "access": "public"
  }
}
